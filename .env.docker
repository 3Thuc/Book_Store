# ============================================================
#  .env.docker – Biến môi trường cho Docker Compose
#  Copy file này ra thành .env nếu muốn docker compose tự đọc
#  Hoặc dùng: docker compose --env-file .env.docker up -d
# ============================================================

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
# MYSQL_PASSWORD=
MYSQL_DB=bookstore

# MinIO
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=admin123456789

# OpenSearch
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=VnBook$2025!Qx9
OPENSEARCH_INDEX=books_current

# Google Books API (để trống nếu không dùng)
GOOGLE_BOOKS_API_KEY=AIzaSyDPq703Q0sJehGBKJasgw3zEVvzn8FM9E4

# Ollama – LLM Model (qwen2.5:7b ~4.7GB | qwen2.5:3b ~2GB nếu RAM thấp)
OLLAMA_MODEL=qwen2.5:7b
